# Torrent Fiesta runtime + compose settings
# Copy this file to .env, then use ONE section below as your base.
# Keep exactly one value per key in your final .env file.

# ======================================================
# macOS Example
# ======================================================
# Root directory that contains:
# data/, prowlarr/, radarr/, sabnzbd/, sonarr/, whisparr/
TF_ROOT=/Volumes/Pteri/torrent_fiesta
TF_TZ=Asia/Bangkok
TF_BROWSER_MODE=chrome
TF_ENABLE_UI_CLOSE=true
TF_TRANSMISSION_MODE=auto
# Optional on macOS (usually not needed)
# TF_TRANSMISSION_EXE=/Applications/Transmission.app
TF_PUID=1000
TF_PGID=1000

# ======================================================
# Windows Example
# ======================================================
# IMPORTANT: Use forward slashes for Docker bind mounts on Windows.
# Example: C:/torrent_fiesta (NOT C:\torrent_fiesta)
# TF_ROOT=C:/torrent_fiesta
# TF_TZ=Asia/Bangkok
# TF_BROWSER_MODE=default
# TF_ENABLE_UI_CLOSE=true
# TF_TRANSMISSION_MODE=auto
# Optional explicit Transmission path if auto-detect fails:
# TF_TRANSMISSION_EXE=C:/Program Files/Transmission/transmission-qt.exe
# TF_PUID=1000
# TF_PGID=1000
