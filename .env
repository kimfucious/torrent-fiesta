# Torrent Fiesta runtime + compose settings
# Copy to .env and adjust for your machine.

# Root directory that contains data/, prowlarr/, radarr/, sabnzbd/, sonarr/.
# Use forward slashes on Windows for Docker bind mounts.

TF_ROOT=/Volumes/Pteri/torrent_fiesta
TF_TZ=Asia/Bangkok
TF_BROWSER_MODE=chrome
TF_ENABLE_UI_CLOSE=true
TF_TRANSMISSION_MODE=auto